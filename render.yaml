services:
  - type: web
    name: revizyon-talebi-demo
    runtime: docker
    healthCheckPath: /
    envVars:
      # Set this after Render creates your service URL.
      - key: APP_BASE_URL
        sync: false
      - key: LLM_PROVIDER
        value: mock

